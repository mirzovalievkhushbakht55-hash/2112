<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SafarGo - Сервис попутчиков</title>
  
  <!-- CSS -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      -webkit-tap-highlight-color: transparent;
    }

    :root {
      --green: #10b981;
      --green-dark: #059669;
      --blue: #2563eb;
      --red: #ef4444;
      --yellow: #f59e0b;
      --text: #0f172a;
      --text-light: #64748b;
      --border: #e5e7eb;
      --bg: #f9fafb;
      --shadow: 0 4px 12px rgba(0, 0, 0, 0.08);
    }

    body {
      font-family: 'Inter', -apple-system, sans-serif;
      background: var(--bg);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 10px;
      -webkit-font-smoothing: antialiased;
    }

    .app {
      width: 100%;
      max-width: 430px;
      height: 95vh;
      max-height: 900px;
      background: #fff;
      border-radius: 36px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
      display: flex;
      flex-direction: column;
      overflow: hidden;
      position: relative;
    }

    /* LOADING */
    #loading {
      position: absolute;
      inset: 0;
      background: #fff;
      z-index: 9999;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 14px;
      border-radius: 36px;
      transition: opacity 0.3s;
    }

    #loading.hidden {
      opacity: 0;
      pointer-events: none;
    }

    .logo-text {
      font-size: 1.5rem;
      font-weight: 900;
      color: var(--green);
    }

    .spinner {
      width: 32px;
      height: 32px;
      border: 3px solid var(--border);
      border-top-color: var(--green);
      border-radius: 50%;
      animation: spin 0.7s linear infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    /* HEADER */
    .header {
      background: #fff;
      padding: 13px 16px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid var(--border);
      flex-shrink: 0;
      z-index: 10;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 7px;
      font-weight: 800;
      font-size: 17px;
      color: var(--text);
    }

    .logo i {
      color: var(--green);
      font-size: 20px;
    }

    .header-icons {
      display: flex;
      gap: 7px;
      align-items: center;
    }

    .icon-btn {
      width: 33px;
      height: 33px;
      border: none;
      background: var(--bg);
      border-radius: 50%;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-light);
      font-size: 14px;
      transition: background 0.15s;
    }

    .icon-btn:hover {
      background: var(--border);
    }

    /* MAIN CONTENT */
    .content {
      flex: 1;
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }

    #app-content {
      flex: 1;
      overflow-y: auto;
      display: flex;
      flex-direction: column;
    }

    /* MAP */
    .map-container {
      width: 100%;
      height: 100%;
      z-index: 1;
    }

    #map {
      width: 100%;
      height: 100%;
    }

    /* SEARCH PANEL */
    .search-panel {
      background: #fff;
      padding: 11px 13px;
      box-shadow: 0 -3px 12px rgba(0, 0, 0, 0.08);
      border-radius: 18px 18px 0 0;
      flex-shrink: 0;
    }

    .search-inputs {
      display: flex;
      gap: 5px;
      align-items: center;
      margin-bottom: 8px;
    }

    .search-input {
      flex: 1;
      padding: 10px 13px;
      border-radius: 10px;
      border: 1.5px solid var(--border);
      font-size: 13px;
      font-family: inherit;
      outline: none;
      transition: border-color 0.15s;
    }

    .search-input:focus {
      border-color: var(--green);
      background: #fff;
    }

    .geo-btn {
      width: 36px;
      height: 36px;
      border: none;
      background: #ecfdf5;
      color: var(--green);
      border-radius: 10px;
      cursor: pointer;
      font-size: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      transition: all 0.15s;
    }

    .geo-btn:hover {
      background: var(--green);
      color: #fff;
    }

    .search-buttons {
      display: flex;
      gap: 6px;
      margin-bottom: 8px;
    }

    .btn-primary {
      flex: 1;
      padding: 10px;
      border-radius: 10px;
      font-weight: 700;
      font-size: 13px;
      border: none;
      cursor: pointer;
      font-family: inherit;
      background: var(--green);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      transition: background 0.15s;
    }

    .btn-primary:hover {
      background: var(--green-dark);
    }

    .btn-secondary {
      flex: 1;
      padding: 10px;
      border-radius: 10px;
      font-weight: 700;
      font-size: 13px;
      border: none;
      cursor: pointer;
      font-family: inherit;
      background: var(--blue);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      transition: background 0.15s;
    }

    .btn-secondary:hover {
      background: #1d4ed8;
    }

    /* NAVBAR */
    .navbar {
      background: #fff;
      border-top: 1px solid var(--border);
      display: flex;
      height: 58px;
      flex-shrink: 0;
      z-index: 20;
    }

    .nav-item {
      flex: 1;
      padding: 8px 4px 6px;
      border: none;
      background: none;
      cursor: pointer;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 2px;
      color: #94a3b8;
      font-size: 9px;
      font-weight: 600;
      font-family: inherit;
      transition: color 0.15s;
      position: relative;
    }

    .nav-item i {
      font-size: 17px;
    }

    .nav-item.active {
      color: var(--green);
    }

    .nav-item.add-btn {
      background: var(--green);
      color: #fff;
      border-radius: 0;
    }

    .nav-item.add-btn i {
      font-size: 19px;
    }

    /* MODAL */
    .modal {
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 1000;
      display: none;
      align-items: flex-end;
      justify-content: center;
      border-radius: 36px;
    }

    .modal.active {
      display: flex;
    }

    .modal-content {
      background: #fff;
      width: 100%;
      border-radius: 22px 22px 0 0;
      max-height: 92%;
      overflow-y: auto;
      animation: slideUp 0.28s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    @keyframes slideUp {
      from { transform: translateY(100%); }
      to { transform: translateY(0); }
    }

    .modal-header {
      padding: 13px 17px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid var(--border);
    }

    .modal-title {
      font-size: 16px;
      font-weight: 800;
    }

    .modal-close {
      width: 28px;
      height: 28px;
      border: none;
      background: var(--bg);
      border-radius: 50%;
      cursor: pointer;
      font-size: 12px;
      color: var(--text);
    }

    .modal-body {
      padding: 13px 17px;
    }

    /* TOAST */
    .notifications {
      position: fixed;
      top: 16px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 99999;
      display: flex;
      flex-direction: column;
      gap: 6px;
      align-items: center;
      pointer-events: none;
    }

    .toast {
      background: var(--text);
      color: #fff;
      padding: 9px 18px;
      border-radius: 22px;
      font-size: 0.8rem;
      font-weight: 700;
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
      animation: toastIn 0.2s ease, toastOut 0.3s 2.5s ease forwards;
    }

    .toast.success {
      background: var(--green);
    }

    .toast.error {
      background: var(--red);
    }

    .toast.warning {
      background: var(--yellow);
      color: var(--text);
    }

    @keyframes toastIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes toastOut {
      to {
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <!-- ГЛАВНЫЙ КОНТЕЙНЕР ПРИЛОЖЕНИЯ -->
  <div class="app">
    
    <!-- LOADING OVERLAY -->
    <div id="loading">
      <div class="logo-text">🚗 SafarGo</div>
      <div class="spinner"></div>
    </div>

    <!-- HEADER -->
    <div class="header">
      <div class="logo">
        <i class="fas fa-route"></i>SafarGo
      </div>
      <div class="header-icons">
        <button class="icon-btn" id="notif-btn" title="Уведомления">
          <i class="fas fa-bell"></i>
        </button>
        <button class="icon-btn" id="profile-btn" title="Профиль">
          <i class="fas fa-user"></i>
        </button>
      </div>
    </div>

    <!-- ОСНОВНОЙ КОНТЕНТ -->
    <div class="content">
      <div id="app-content"></div>
    </div>

    <!-- NAVBAR -->
    <div class="navbar">
      <button class="nav-item active" data-screen="home">
        <i class="fas fa-home"></i>
        <span>Главная</span>
      </button>
      <button class="nav-item" data-screen="trips">
        <i class="fas fa-route"></i>
        <span>Поездки</span>
      </button>
      <button class="nav-item add-btn" data-screen="add-choice">
        <i class="fas fa-plus"></i>
      </button>
      <button class="nav-item" data-screen="profile">
        <i class="fas fa-user"></i>
        <span>Профиль</span>
      </button>
      <button class="nav-item" data-screen="help">
        <i class="fas fa-question"></i>
        <span>Помощь</span>
      </button>
    </div>

  </div>

  <!-- NOTIFICATIONS CONTAINER -->
  <div class="notifications" id="notifications"></div>

  <!-- EXTERNAL LIBRARIES -->
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.39.0/+esm" type="module"></script>

  <!-- APP ENTRY POINT -->
  <script type="module" src="./index.js"></script>
</body>
</html>